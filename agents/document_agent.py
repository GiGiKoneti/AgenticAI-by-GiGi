class DocumentAgent:
    def run(self, details):
        """
        Will later: extract text from PDF / image,
        use OCR + LLM to find dates, tasks, deadlines.
        """
        return {
            "summary": "DocumentAgent placeholder response",
            "details_received": details,
        }